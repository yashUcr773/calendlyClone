export interface MeetingFormData {
    location: string
    locationURL: string
    themeColor: string
    eventName: string
    duration: number
}

export interface MeetingEventItemType {
    location: string
    locationURL: string
    themeColor: string
    eventName: string
    duration: number
    createdBy: string
    id:string
}